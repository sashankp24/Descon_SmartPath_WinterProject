keep this files in one folder 
open this folder in command prompt
type this one command prompt "uvicorn api_simple:app --reload --host 127.0.0.1 --port 8000"
type this in another command prompt "python -m http.server 8001"
now go to this link for map "http://127.0.0.1:8001/navigation_map.html"


datasets are from metr la this is choosen because this is easily available and organised
